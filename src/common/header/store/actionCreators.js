import { fromJS } from 'immutable';
import * as constants from './constants';




export const toggleCollapsed = () => {
    return  {
        type: constants.LEFT_SIDE_TOGGLE_COLLAPSEDE
       
    }
}